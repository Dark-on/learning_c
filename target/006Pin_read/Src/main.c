/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2022 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>


int main(void)
{
	// base address RCC:  0x4002 1000
	uint32_t volatile *const pClkCtrlReg =        (uint32_t *) 0x4002104C;
	uint32_t volatile *const pPortBModeReg =      (uint32_t *) 0x48000400;
	uint32_t volatile *const pPortBOutReg =       (uint32_t *) 0x48000414;
	uint32_t volatile *const pPortCModeReg =      (uint32_t *) 0x48000800;
	uint32_t const volatile *const pPortCInpReg = (uint32_t *) 0x48000810;

	// 1. enable the clock
	// a. for GPIOB peripheral in the AHB2ENR
	*pClkCtrlReg |= (1 << 1);
	// b. for GPIOC peripheral in the AHB2ENR
	*pClkCtrlReg |= (1 << 2);

	// 2. configure the mode of the IO pin PB13 as output
	// a. CLEAR the 26th and 27th bit positions
	*pPortBModeReg &= ~(0b11 << 26);
	// b. SET the 26th bit position
	*pPortBModeReg |= (1 << 26);

	// 3. configure the mode of the IO pin PC13 as input
	// a. CLEAR the 26th and 27th bit positions
	*pPortCModeReg &= ~(0b11 << 26);

	while(1){
		// 4. READ 13th bit of the input data register
		uint8_t pinStatus = (uint8_t)((*pPortCInpReg >> 13) & 0x1);

		if (pinStatus){
			// 5. Turn ON the LED
			*pPortBOutReg |= (1 << 13);
		} else {
			// 6. Turn OFF the LED
			*pPortBOutReg &= ~(1 << 13);
		}
	} // loop
}
